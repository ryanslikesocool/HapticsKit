namespace HapticsKit {
    public enum NotificationType {
        Success = 0,
        Warning = 1,
        Error = 2
    }

    public enum ImpactType {
        Light = 0,
        Medium = 1,
        Heavy = 2,
        Soft = 3,
        Rigid = 4
    }
}